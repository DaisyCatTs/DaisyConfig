## [Unreleased]
### Added
- nested section helpers on `DaisyFieldReader`
- `DaisyValidation` helpers for common config checks
- `DaisyConfigBundleHandle` for multi-file config orchestration
- `DaisyYamlBundle` and `yamlConfigBundleHandle(...)`
- `ensureDefaultConfigResources(...)`
- live `asDaisyTextSource()` adapters for config handles and bundle handles
- `mergeTextConfigs(...)`
- stronger Phase 2 example plugin with multi-file config and text bundles

## [0.1.0-SNAPSHOT]
### Added
- DaisyConfig repo skeleton
- config-base
- config-yaml
- config-daisycore
- config-all
- example plugin
- README and migration docs
